@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Ocurrió un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de Solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de Desarrollo</h3>
<p>
    Cambiar al entorno <strong>Desarrollo</strong> mostrará información más detallada sobre el error que ocurrió.
</p>
<p>
    <strong>El entorno de Desarrollo no debe estar habilitado para aplicaciones desplegadas.</strong>
    Puede resultar en la exhibición de información sensible de excepciones a los usuarios finales.
    Para depuración local, habilite el entorno <strong>Desarrollo</strong> estableciendo la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> a <strong>Desarrollo</strong>
    y reiniciando la aplicación.
</p>
